services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app  # Mount source for live reloading
    environment:
      - CHAINLIT_HOST=0.0.0.0
      - DISABLE_WATCH=true
      - TERM=xterm
    restart: unless-stopped